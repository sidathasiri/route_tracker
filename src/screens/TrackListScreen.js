import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TractListScreen = () => {
  return <Text style={{ fontSize: 48 }}>TractListScreen</Text>;
};

const styles = StyleSheet.create({});

export default TractListScreen;
